<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    tools:context="com.example.mybuttontutorial.RatingBarActivity" >

    <!-- wrap_content for width is required to keep the no of starts as listed in numStars.
    if the width set otherwise the number of stars should change accordingly. -->
    <RatingBar 
        android:id="@+id/ratingbar"
        android:layout_height="wrap_content"
        android:layout_width="wrap_content"
        android:stepSize="1"
        android:numStars="5"/>

</RelativeLayout>
